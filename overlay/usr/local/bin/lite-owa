#!/bin/bash
# Open Outlook Web App as its own window
. /usr/share/ubuntu-lite/lite.conf
[ -n "${OWA_URL:-}" ] || { echo "OWA_URL is not configured in this image (config/build.env)"; exit 1; }
exec chrome --app="$OWA_URL" "$@"
