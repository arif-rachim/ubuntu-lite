# Firmware installed when FIRMWARE_MODE=select (default). Ubuntu 24.04 splits
# linux-firmware into per-vendor packages, so we pick packages instead of
# pruning files; apt upgrades keep the selection. ~110 MB instead of ~650 MB.
linux-firmware-intel-graphics
linux-firmware-amd-graphics
linux-firmware-amd-misc
linux-firmware-intel-misc
linux-firmware-realtek
linux-firmware-misc
firmware-sof-signed
# Wi-Fi: Broadcom (MacBook Pro 2016 BCM43602, many older laptops), ~11 MB.
linux-firmware-broadcom-wireless
# Intel Wi-Fi is ~110 MB; uncomment for Intel laptops (iwlwifi):
# linux-firmware-intel-wireless
# Not included on purpose: linux-firmware-nvidia-graphics, mediatek, qualcomm,
# marvell, mellanox, qlogic, netronome. Add them here if a machine needs one.
