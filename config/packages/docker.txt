# Docker Engine from download.docker.com (stable). All dev toolchains live in
# containers, so the host never gets node/python/java packages.
docker-ce
docker-ce-cli
containerd.io
docker-buildx-plugin
docker-compose-plugin
